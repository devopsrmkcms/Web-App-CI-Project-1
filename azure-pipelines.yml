
trigger:
- main

pool:
  vmImage: ubuntu-latest

- task: Docker@2
  inputs:
    containerRegistry: 'docker-registry-login'
    repository: 'rmk1988/httpd-webapp'
    command: 'buildAndPush'
    Dockerfile: '**/Dockerfile'
